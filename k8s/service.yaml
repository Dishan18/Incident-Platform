apiVersion: v1
kind: Service

metadata:
  name: ticketing-platform-service

spec:
  selector:
    app: ticketing-platform

  ports:
  - protocol: TCP
    port: 8501
    targetPort: 8501

  type: NodePort